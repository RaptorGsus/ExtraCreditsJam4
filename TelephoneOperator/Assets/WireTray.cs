using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WireTray : MonoBehaviour
{
    public void ResetWires()
    {
        //var wires = GetComponentsInChildren<TrayWire>();
        //foreach(TrayWire w in wires) {
        //    w.spawnedWire.GetComponent<Wire>().ResetWire();
        //}
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
